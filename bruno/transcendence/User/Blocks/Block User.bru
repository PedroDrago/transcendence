meta {
  name: Block User
  type: http
  seq: 2
  tags: [
    user, blocks
  ]
}

post {
  url: {{user_base}}/users/blocks
}

headers {
  x-user-id: {{user_id}}
}

body:json {
  {
    "blockedId": "TARGET_UUID_HERE"
  }
}
