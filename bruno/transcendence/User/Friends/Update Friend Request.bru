meta {
  name: Update Friend Request
  type: http
  seq: 4
  tags: [
    user, friends
  ]
}

patch {
  url: {{user_base}}/users/friends/requests/{{last_request_id}}
}

headers {
  x-user-id: {{user_id}}
}

body:json {
  {
    "status": "ACCEPTED"
  }
}
